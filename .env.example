# База основного API (без хвоста /api/v1 — он обрежется при необходимости)
VITE_API_BASE_URL=http://localhost:1234
# Опционально: отдельный URL для сервиса посещаемости (если пусто — используется VITE_API_BASE_URL)
VITE_ATTENDANCE_API_BASE=
# Расходы: true — только локальное хранилище; false или не задано — API /api/v1/expenses
# VITE_EXPENSES_OFFLINE=true